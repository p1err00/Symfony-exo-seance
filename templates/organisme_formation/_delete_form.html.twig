<form method="post" action="{{ path('app_organisme_formation_delete', {'id': organisme_formation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ organisme_formation.id) }}">
    <button class="btn">Delete</button>
</form>
